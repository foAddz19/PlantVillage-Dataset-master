# เครื่องตรวจโรคพืชพร้อมแผนที่ — จำลองบนคอมพิวเตอร์

ดับเบิลคลิก **start_device.bat** หรือเปิด **http://127.0.0.1:8765/device** เมื่อโปรแกรมทำงานอยู่

## ทดลองให้ครบขั้นตอน

1. เลือก **ใช้ภาพทดลอง** เพื่อใช้ภาพใน PlantVillage หรือ **เลือกไฟล์** จากเครื่อง หากมีเว็บแคมให้กด **เปิดกล้อง** และอนุญาตในเบราว์เซอร์
2. ใน **ตั้งค่าจำลอง** ระบุพิกัด หรือกด **ขยับไปจุดจำลองถัดไป** ค่าเริ่มต้นเป็นจุดสมมติในจังหวัดร้อยเอ็ด ไม่ใช่ตำแหน่งจริงของเครื่องหรือที่ตั้งวิทยาลัย
3. กลับไป **ตรวจใบพืช** แล้วกด **ถ่าย / ตรวจโรค** ระบบใช้ AI จริงบนคอมพิวเตอร์ ผลไม่ได้กำหนดไว้ล่วงหน้า
4. ดูผล 3 อันดับ หากผลยังไม่ชัดเจนจะบอก **ยังระบุไม่ได้** สามารถระบุความรุนแรงตามที่ผู้ตรวจสังเกตและใส่บันทึกเพิ่มเติม
5. กด **บันทึกจุดตรวจ** ภาพ ผล AI รุ่นโมเดล เวลา และพิกัดที่ใช้ตอนส่งภาพตรวจ จะถูกเก็บในฐานข้อมูล
6. เปิด **แผนที่จุดตรวจ** เพื่อดูจุดที่บันทึก กดรายการด้านขวาเพื่อดูภาพและรายละเอียด
7. ไปหน้า **ประวัติ** เพื่อดูรายการย้อนหลัง หรือส่งออก **CSV** สำหรับตาราง และ **GeoJSON** สำหรับโปรแกรม GIS

การเปลี่ยนพิกัดหลังตรวจภาพแล้วมีผลกับภาพถัดไปเท่านั้น ไม่เปลี่ยนพิกัดของผลเดิม การกดบันทึกซ้ำไม่สร้างรายการซ้ำและไม่เขียนทับบันทึกครั้งแรก

## แผนที่และสีของจุด

แผนที่เริ่มต้นแสดงพิกัดบนพื้นกริดแบบออฟไลน์ สามารถเลื่อนและซูมได้ แต่ไม่มีถนนหรือขอบเขตแปลงจริง เมื่อกด **เปิดแผนที่ถนน (อินเทอร์เน็ต)** จึงโหลดพื้นแผนที่ OpenStreetMap ผ่านอินเทอร์เน็ต ภาพใบพืชและผลตรวจยังอยู่ในเครื่อง แผนที่ถนนอาจใช้ไม่ได้เมื่อไม่มีเครือข่าย แต่จุดตรวจและประวัติยังใช้งานได้

- แดง: ผู้ตรวจระบุว่ารุนแรง
- เหลือง: ผู้ตรวจระบุว่าเล็กน้อย
- เขียว: AI คาดว่าใบปกติ และผู้ตรวจไม่ได้ระบุความรุนแรง
- เทา: ผลไม่ชัดเจน หรือพบกลุ่มโรคแต่ยังไม่มีการประเมินความรุนแรง

คะแนนโมเดลไม่ใช่ระดับความรุนแรงของโรค ทุกจุดมีข้อมูลกำกับว่าใช้ **พิกัดจำลอง** หรือ **พิกัดที่ระบุเอง** วงแหวนเส้นประเป็นตำแหน่งปัจจุบัน ไม่ใช่รายการบันทึก

Leaflet เก็บไว้ใน `static/vendor/leaflet` พร้อมใบอนุญาต ไม่ต้องโหลดไลบรารีจาก CDN ตอนเปิดแอป การใช้พื้นแผนที่ออนไลน์เป็นการดูตามหน้าจอเท่านั้น ไม่มีการดาวน์โหลดพื้นที่ล่วงหน้า อ้างอิง [นโยบายแผนที่ OpenStreetMap](https://operations.osmfoundation.org/policies/tiles/)

## สิ่งที่ทำงานจริงและสิ่งที่จำลอง

| ส่วน | บนคอมพิวเตอร์ตอนนี้ |
|---|---|
| AI | ใช้โมเดลที่ฝึกแล้ว ประมวลผลจริงในเครื่อง |
| กล้อง | ใช้เว็บแคม / USB ผ่านเบราว์เซอร์ หรือไฟล์ภาพ |
| GPS | พิกัดจำลองหรือระบุเอง ยังไม่ได้อ่านโมดูล GPS |
| LED | ปุ่มและสถานะจำลอง ไม่ได้ควบคุมไฟจริงหรือแก้ความสว่างของภาพ |
| แบตเตอรี่ | ตัวเลื่อนค่าจำลอง ไม่ใช่การวัดแบตเตอรี่จริง |
| บันทึก / ประวัติ | SQLite จริง ข้อมูลคงอยู่หลังปิดโปรแกรม |
| แผนที่ | จุดพิกัดจริงตามค่าที่ป้อน; พื้นถนนออนไลน์เปิดได้ตามต้องการ |

ทดสอบการจัดหน้าจอขนาด **800 × 480** และหน้าจอคอมแล้ว ปุ่มหลักรองรับการแตะ กด **เต็มหน้าจอ** หรือ F11 สำหรับการสาธิต ระบบกล้องจะหยุดเมื่อเปลี่ยนหน้าในเครื่องจำลอง ออกจากแท็บ หรือปิดหน้าเว็บ

## ข้อมูลและการสำรอง

ฐานข้อมูลอยู่ที่ **data/scans.sqlite3** เก็บทั้งภาพและผลตรวจ โหมดเครื่องจำลองเก็บภาพที่ตรวจเป็นฉบับร่างไว้ในเครื่อง และจะนับเป็นรายการประวัติเมื่อกดบันทึก ฉบับร่างที่ไม่ได้บันทึกเกิน 24 ชั่วโมงจะถูกล้างเมื่อมีการตรวจภาพครั้งใหม่ รายการที่บันทึกแล้วไม่ถูกลบอัตโนมัติ

สำรองข้อมูลโดยหยุดโปรแกรมด้วย **stop.bat** ก่อน แล้วคัดลอกโฟลเดอร์ **data** ทั้งโฟลเดอร์ CSV/GeoJSON ไม่มีไฟล์ภาพแนบ จึงควรเก็บฐานข้อมูลด้วยหากต้องการภาพย้อนหลัง ไม่ควรเปิดไฟล์ฐานข้อมูลที่มาจากแหล่งไม่รู้จักแทนข้อมูลของตัวเอง

## เตรียมย้ายไป Raspberry Pi 5

ซอฟต์แวร์แยกหน้าจอ (`templates/device.html`, `static/device.js`), บันทึกข้อมูล (`leaflab/scans.py`), API (`leaflab/device.py`) และโมเดล AI ออกจากกันแล้ว สามารถเริ่มจากเว็บแคม USB บน Pi และเปิดหน้าจอเดียวกันผ่าน localhost ได้หลังติดตั้งสภาพแวดล้อมที่เข้ากัน

เมื่ออุปกรณ์มาถึง งานที่ยังต้องทำและทดสอบบนเครื่องจริงคือ:

1. ตรวจ Raspberry Pi OS แบบ 64 บิต รุ่น Python, NumPy และ scikit-learn ให้รองรับโมเดลที่บันทึกจากคอม ย้ายเฉพาะไฟล์แอป โมเดล และข้อมูลที่ต้องใช้ ไม่คัดลอก `.venv` ของ Windows ไปใช้บน Linux
2. ทดสอบความเร็วและหน่วยความจำของโมเดลบน Pi ก่อนใช้งานภาคสนาม การฝึกโมเดลทำบนคอมแล้วคัดลอกไฟล์ไปใช้ได้ ไม่จำเป็นต้องฝึกบน Pi
3. ถ้าใช้กล้อง CSI ให้เพิ่มตัวรับภาพจาก **Picamera2** แทนกล้องเบราว์เซอร์ ตาม [เอกสารกล้อง Raspberry Pi](https://www.raspberrypi.com/documentation/computers/camera_software.html) การรองรับเว็บแคมบนคอมไม่ได้ยืนยันว่ากล้อง CSI จะใช้งานได้ทันที
4. เพิ่มตัวอ่านพิกัดจากโมดูล GPS ที่เลือก และบันทึกสถานะ fix/เวลา/ความสดของพิกัด จึงค่อยเปิดแหล่งพิกัด `gps` จริง ปัจจุบัน API รับเฉพาะ simulated/manual
5. ต่อและทดสอบตัวควบคุมไฟ LED และตัววัดแบตเตอรี่ตามวงจรจริง ปุ่มจำลองในแอปยังไม่สั่ง GPIO

ขณะนี้ยังไม่ได้ทดสอบบน Raspberry Pi 5 หรือวงจรจริง ความเร็วบนคอมและผลภาพจำลองไม่ใช่ผลรับรองการใช้งานภาคสนาม

## ทดสอบซอฟต์แวร์

```powershell
.\.venv\Scripts\python.exe -B -m unittest discover -s tests -v
.\.venv\Scripts\python.exe -B tests/browser_device.py
```

การทดสอบเบราว์เซอร์เปิดเซิร์ฟเวอร์ชั่วคราวที่พอร์ต 8766 ใช้ฐานข้อมูลทดสอบแยก ใช้กล้องจำลอง แต่เรียกโมเดล AI จริง ไม่เปิดฮาร์ดแวร์จริงและไม่ส่งคำขอพื้นแผนที่ไป OpenStreetMap
